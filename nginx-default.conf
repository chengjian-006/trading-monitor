# Catch-all listener: do not serve the application from a bare IP address or
# an unrecognised Host header.  Named HTTPS virtual hosts live in
# nginx-site.conf.
server {
    listen 80 default_server;
    server_name _;
    return 444;
}
